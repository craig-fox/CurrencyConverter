'use strict';

describe('converterApp.history module', function() {

  beforeEach(module('converterApp.history'));

  describe('HistoryController', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var historyController = $controller('historyController');
      expect(historyController).toBeDefined();
    }));

  });
});